from datetime import timedelta


class UnitsVelocity:
    spear = timedelta(minutes=16, seconds=40)
    sword = timedelta(minutes=20, seconds=22)
    axe = timedelta(minutes=16, seconds=40)
    spy = timedelta(minutes=8, seconds=20)
    light = timedelta(minutes=9, seconds=16)
    heavy = timedelta(minutes=10, seconds=11)
    ram = timedelta(minutes=27, seconds=47)
    catapult = timedelta(minutes=27, seconds=47)
    gentry = timedelta(minutes=32, seconds=24)
